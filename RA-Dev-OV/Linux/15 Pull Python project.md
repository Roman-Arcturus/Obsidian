Win version

cd ~/Projects
git clone https://github.com/Roman-Arcturus/learn_python.git
cd learn_python

git clone git@github.com:Roman-Arcturus/learn_python.git
cd learn_python

python -m venv .venv
```
PS}
.\.venv\Scripts\Activate.ps1  

Bash}
source .venv/Scripts/activate 
```
pip install --upgrade pip setuptools wheel
pip install -e .
python -m pip install --upgrade pip
 
python -m learn_python.phase1_condition
pytest -q


```
git fetch origin
git status
git pull origin main  
```

